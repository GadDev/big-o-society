import { describe, expect, it } from 'vitest'; import { alienOrder } from '../../src/challenges/graph-traversal/10-alien-dictionary'; describe('Graph 10',()=>{it('builds precedence graph from first differing chars',()=>expect(alienOrder(['wrt','wrf','er','ett','rftt'])).toBe('wertf')); it('rejects cycle and invalid prefix',()=>{expect(alienOrder(['z','x','z'])).toBe(''); expect(alienOrder(['abc','ab'])).toBe('');});});
