/**
 * Exercise 03 — Remove Duplicates In Place
 * Difficulty: Easy | Mastery: Level 2 — Implement
 *
 * Goal: given an ascending sorted array `nums`, remove duplicates in place
 * and return the count `k` of unique values, with those values in the first
 * k positions.
 * Example: [1, 1, 2] -> k = 2, first k values = [1, 2];
 *          [0, 0, 1, 1, 1, 2, 2, 3, 3, 4] -> k = 5, first k values = [0, 1, 2, 3, 4];
 *          [5, 5, 5, 5] -> k = 1, first k values = [5].
 *
 * Given an ascending sorted array, remove duplicates in place so that every
 * unique value appears once. Return the number of unique values `k`.
 * The first k positions of nums must contain the unique values in order.
 *
 * Example:
 *   [1, 1, 2, 2, 3] -> k = 3, first k values = [1, 2, 3]
 *
 * Constraints:
 *   - O(n) time
 *   - O(1) auxiliary space
 *   - Mutate the original array; do not create a second result array.
 *
 * Before coding, identify the roles:
 *   - Which pointer READS new candidates?
 *   - Which pointer WRITES the next accepted value?
 */
export function removeDuplicates(nums: number[]): number {
	// TODO: implement using read/write pointers.
	throw new Error("Not implemented");
}
