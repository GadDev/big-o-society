/**
 * Exercise 02 — Remove Adjacent Duplicates
 * Difficulty: Easy | Mastery: L1→L2
 * Skill: Stack as editable history
 *
 * Recognition trigger: undo the latest character when a matching neighbor arrives
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Stack?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   Why is only the most recent unresolved character relevant?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(n)
 */

export function removeAdjacentDuplicates(s: string): string {
  // TODO: Repeatedly remove adjacent equal characters until no such pair remains.
  throw new Error('Not implemented');
}
