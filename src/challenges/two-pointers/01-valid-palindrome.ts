/**
 * Exercise 01 — Valid Palindrome
 * Difficulty: Easy | Mastery: Level 1 — Recognize
 *
 * Return true when `text` reads the same forward and backward after ignoring
 * non-alphanumeric characters and letter casing.
 *
 * Examples:
 *   "racecar" -> true
 *   "A man, a plan, a canal: Panama" -> true
 *   "hello" -> false
 *
 * Constraints:
 *   - O(n) time
 *   - O(1) auxiliary space
 *   - Do not create a fully normalized copy of the input string.
 *
 * Before coding, answer aloud:
 *   1. Where should the two pointers start?
 *   2. What should make either pointer move without comparing?
 *   3. When can you return false immediately?
 */
export function isPalindrome(text: string): boolean {
	// TODO: implement using two pointers from opposite ends.
	throw new Error("Not implemented");
}
