/**
 * Exercise 01 — Contains Duplicate
 * Difficulty: Easy | Mastery: L1 Recognize
 * Variant: Set membership
 *
 * Recognition trigger: seen before -> Set
 *
 * Before coding, say aloud:
 *   1. What clue points to Hash Map?
 *   2. What state/history must remain true while I scan?
 *
 * Critical explanation question:
 *   What information must you remember to detect a duplicate immediately?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(n)
 */

export function containsDuplicate(nums: number[]): boolean {
	// TODO: remember values as you scan.
	throw new Error("Not implemented");
}
