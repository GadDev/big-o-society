import { TreeNode } from './structures';

/**
 * Exercise 1 — Binary Tree Level Order Traversal
 * Difficulty: Easy | Mastery: L1 Recognize
 * Skill: Tree BFS by levels
 *
 * Problem:
 *   Return node values level by level from left to right.
 *
 * Recognition trigger: tree output grouped by depth -> queue one level at a time
 *
 * Before coding, say aloud:
 *   1. What clue points to Breadth-First Search (BFS)?
 *   2. What state/invariant must stay true after every move or recursive call?
 *
 * Critical explanation question:
 *   Why is the next choice/expansion/state transition safe, and what work does it eliminate?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(w)
 */

export function levelOrder(root: TreeNode | null): number[][] {
  // TODO: Return node values level by level from left to right.
  throw new Error('Not implemented');
}
