/**
 * Exercise 07 — Three Sum
 * Difficulty: Medium | Mastery: Level 4 — Adapt
 *
 * Return every unique triplet [a, b, c] such that a + b + c === 0.
 * The input does not need to be sorted. The result must not contain duplicate
 * triplets.
 *
 * Example:
 *   [-1, 0, 1, 2, -1, -4] -> [[-1, -1, 2], [-1, 0, 1]]
 *
 * Constraints:
 *   - Aim for O(n^2) time
 *   - Sorting is allowed
 *   - Do not use a cubic triple loop.
 *
 * Recognition challenge:
 *   The problem asks for THREE values. How can you freeze one value so the
 *   remaining problem becomes a two-pointer problem?
 */
export function threeSum(nums: number[]): number[][] {
	// TODO: combine sorting + an outer loop + two pointers.
	throw new Error("Not implemented");
}
