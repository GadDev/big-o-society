/**
 * Exercise 05 — Squares of a Sorted Array
 * Difficulty: Medium | Mastery: Level 2 → 3
 *
 * Goal: given an ascending sorted array `nums` that may contain negative
 * numbers, return a new array of each value squared, itself in ascending order.
 * Example: [-4, -1, 0, 3, 10] -> [0, 1, 9, 16, 100];
 *          [-7, -3, 2, 3, 11] -> [4, 9, 9, 49, 121];
 *          [0, 2, 8] -> [0, 4, 64].
 *
 * Given an ascending sorted array that may contain negative numbers, return a
 * new array containing each value squared, also in ascending order.
 *
 * Example:
 *   [-4, -1, 0, 3, 10] -> [0, 1, 9, 16, 100]
 *
 * Constraints:
 *   - O(n) time
 *   - O(n) output space only
 *   - Do not square everything and then call .sort().
 *
 * Explain afterward:
 *   Why can the largest remaining square only come from one of the two ends?
 */
export function sortedSquares(nums: number[]): number[] {
	// TODO: implement with opposite-end pointers and fill the output backwards.
	throw new Error("Not implemented");
}
