/**
 * Exercise 04 — Move Zeroes
 * Difficulty: Easy | Mastery: Level 2 — Implement
 *
 * Goal: given an array `nums`, move all zeroes to the end in place while
 * preserving the relative order of the non-zero values.
 * Example: [0, 1, 0, 3, 12] -> [1, 3, 12, 0, 0];
 *          [1, 2, 3] -> [1, 2, 3] (no zeroes, unchanged);
 *          [0, 0, 0] -> [0, 0, 0] (all zeroes, unchanged).
 *
 * Move all zeroes to the end of the array in place while preserving the
 * relative order of all non-zero values.
 *
 * Example:
 *   [0, 1, 0, 3, 12] -> [1, 3, 12, 0, 0]
 *
 * Constraints:
 *   - O(n) time
 *   - O(1) auxiliary space
 *   - Mutate nums in place.
 *
 * Before coding, decide what your slow/write pointer represents.
 */
export function moveZeroes(nums: number[]): void {
	// TODO: implement using read/write pointers.
	throw new Error("Not implemented");
}
