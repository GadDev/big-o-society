import { describe, expect, it } from 'vitest'; import { minReorder } from '../../src/challenges/graph-traversal/07-reorder-routes-to-make-all-paths-lead-to-zero'; describe('Graph 07',()=>{it('counts original edges pointing away from zero during traversal',()=>expect(minReorder(6,[[0,1],[1,3],[2,3],[4,0],[4,5]])).toBe(3)); it('handles already-correct roads',()=>expect(minReorder(3,[[1,0],[2,0]])).toBe(0));});
