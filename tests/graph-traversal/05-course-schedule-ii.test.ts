import { describe, expect, it } from 'vitest'; import { findOrder } from '../../src/challenges/graph-traversal/05-course-schedule-ii'; describe('Graph 05',()=>{it('returns a valid topological order',()=>{const r=findOrder(4,[[1,0],[2,0],[3,1],[3,2]]); expect(r).toHaveLength(4); const p=new Map(r.map((v,i)=>[v,i])); expect(p.get(0)!).toBeLessThan(p.get(1)!); expect(p.get(0)!).toBeLessThan(p.get(2)!); expect(p.get(1)!).toBeLessThan(p.get(3)!);}); it('returns [] for a cycle',()=>expect(findOrder(2,[[1,0],[0,1]])).toEqual([]));});
