import { TreeNode } from '../../src/challenges/dfs/structures';
import { describe, expect, it } from 'vitest';import { invertTree } from '../../src/challenges/dfs/02-invert-binary-tree';describe('02 — Invert Tree',()=>{it('inverts recursively',()=>{const r=new TreeNode(4,new TreeNode(2,new TreeNode(1),new TreeNode(3)),new TreeNode(7,new TreeNode(6),new TreeNode(9)));const out=invertTree(r)!;expect(out.left?.val).toBe(7);expect(out.right?.val).toBe(2);expect(out.left?.left?.val).toBe(9);expect(out.right?.right?.val).toBe(1);});it('handles null',()=>expect(invertTree(null)).toBeNull());});
