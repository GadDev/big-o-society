/**
 * Exercise 01 — Classic Binary Search
 * Difficulty: Easy | Mastery: L1 Recognize
 * Skill: Exact lookup
 *
 * Recognition trigger: sorted data + exact target -> halve search space
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Binary Search?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   Which half can be proven impossible after one comparison?
 *
 * Complexity target:
 *   Time: O(log n)
 *   Space: O(1)
 */

export function binarySearch(nums: number[], target: number): number {
  // TODO: Return the index of target in sorted nums, or -1.
  throw new Error('Not implemented');
}
