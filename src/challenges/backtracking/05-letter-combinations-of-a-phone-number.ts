/**
 * Exercise 5 — Letter Combinations of a Phone Number
 * Difficulty: Medium | Mastery: L3 Explain
 * Skill: Decision tree expansion
 *
 * Problem:
 *   Map digits 2-9 to phone keypad letters and return every possible string. Empty input returns [].
 *
 * Recognition trigger: each input symbol offers a small set of choices -> product-shaped search tree
 *
 * Before coding, say aloud:
 *   1. What clue points to Backtracking?
 *   2. What state/invariant must stay true after every move or recursive call?
 *
 * Critical explanation question:
 *   Why is the next choice/expansion/state transition safe, and what work does it eliminate?
 *
 * Complexity target:
 *   Time: O(4^n × n)
 *   Space: O(n) recursion excluding output
 */

export function letterCombinations(digits: string): string[] {
  // TODO: Map digits 2-9 to phone keypad letters and return every possible string. Empty input returns [].
  throw new Error('Not implemented');
}
