/**
 * Exercise 02 — Invert Binary Tree
 * Difficulty: Easy | Mastery: L1→L2
 * Skill: Tree mutation DFS
 *
 * Recognition trigger: same operation independently applies to every subtree
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Depth-First Search (DFS)?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   At what point can left and right children be swapped safely?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(h)
 */

import { TreeNode } from './structures';
export { TreeNode } from './structures';
export function invertTree(root: TreeNode | null): TreeNode | null {
  // TODO: swap every node's left and right subtrees and return root.
  void root; throw new Error('Not implemented');
}
