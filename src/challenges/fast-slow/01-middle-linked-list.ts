/**
 * Exercise 01 — Middle of the Linked List
 * Difficulty: Easy | Mastery: L1 Recognize
 * Skill: 1x / 2x speed
 *
 * Recognition trigger: need midpoint in one pass -> slow 1 step, fast 2
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Fast & Slow Pointers?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   When fast reaches the end, why must slow be at the middle?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(1)
 */

import { ListNode } from './list-node';
export { ListNode } from './list-node';

export function middleNode(head: ListNode | null): ListNode | null {
  // TODO: Return the middle node; for even length return the second middle.
  throw new Error('Not implemented');
}
