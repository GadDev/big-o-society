import { describe, expect, it } from 'vitest';import { GraphNode } from '../../src/challenges/dfs/structures';import { cloneGraph } from '../../src/challenges/dfs/07-clone-graph';describe('07 — Clone Graph',()=>{it('deep clones cycles',()=>{const a=new GraphNode(1),b=new GraphNode(2),c=new GraphNode(3),d=new GraphNode(4);a.neighbors=[b,d];b.neighbors=[a,c];c.neighbors=[b,d];d.neighbors=[a,c];const x=cloneGraph(a)!;expect(x).not.toBe(a);expect(x.val).toBe(1);expect(x.neighbors.map(n=>n.val).sort()).toEqual([2,4]);expect(x.neighbors[0]).not.toBe(b);expect(x.neighbors[0].neighbors.some(n=>n===x)).toBe(true);});it('handles null',()=>expect(cloneGraph(null)).toBeNull());});
