/**
 * Exercise 10 — Four Sum (Boss Fight)
 * Difficulty: Hard | Mastery: Level 5 — Combine
 *
 * Return every unique quadruplet [a, b, c, d] whose values sum to target.
 * The input does not need to be sorted and the result must contain no duplicate
 * quadruplets.
 *
 * Example:
 *   nums = [1,0,-1,0,-2,2], target = 0
 *   -> [[-2,-1,1,2], [-2,0,0,2], [-1,0,0,1]]
 *
 * Constraints:
 *   - Aim for O(n^3) time
 *   - Sorting is allowed
 *   - Do not use four nested loops.
 *
 * Boss-fight questions:
 *   1. How can fixing two values reduce this to Two Sum on a sorted range?
 *   2. At which FOUR places can duplicate results sneak in?
 *   3. What pruning can you safely add after the correct solution works?
 */
export function fourSum(nums: number[], target: number): number[][] {
	// TODO: combine sorting + two outer loops + two pointers + duplicate skipping.
	throw new Error("Not implemented");
}
