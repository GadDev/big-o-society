/**
 * Exercise 05 — Integer Square Root
 * Difficulty: Easy | Mastery: L3 Explain
 * Skill: Binary search numeric answer
 *
 * Recognition trigger: largest integer satisfying x² <= n
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Binary Search?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   How can you compare safely without relying on floating point?
 *
 * Complexity target:
 *   Time: O(log n)
 *   Space: O(1)
 */

export function mySqrt(x: number): number {
  // TODO: Return floor(sqrt(x)) without using Math.sqrt as the algorithm.
  throw new Error('Not implemented');
}
