/**
 * Exercise 04 — First Unique Character in a Stream
 * Difficulty: Medium | Mastery: L2→L3
 * Skill: Queue + frequency map
 *
 * Recognition trigger: preserve arrival order while counts change
 *
 * Before coding, say aloud:
 *   1. Why does this problem suggest Queue / Deque?
 *   2. What invariant/state will stay true while I scan or recurse?
 *
 * Critical explanation question:
 *   Why can repeated characters be lazily removed only from the front?
 *
 * Complexity target:
 *   Time: O(n)
 *   Space: O(u)
 */

export function firstUniqueInStream(chars: string[]): Array<string | null> {
  // TODO: After each arriving character, report the first character seen exactly once so far, or null.
  throw new Error('Not implemented');
}
