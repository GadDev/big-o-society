/**
 * Exercise 02 — Pair Sum in a Sorted Array
 * Difficulty: Easy | Mastery: Level 1 — Recognize
 *
 * Given an ascending sorted array and a target, return the indices of two
 * different values whose sum equals the target. Return null if no pair exists.
 *
 * Example:
 *   nums = [1, 2, 4, 6, 10], target = 8 -> [1, 3]
 *
 * Constraints:
 *   - O(n) time
 *   - O(1) auxiliary space
 *   - Do not use a Map or Set.
 *
 * Before coding, answer aloud:
 *   1. What does the sorted order tell you when the sum is too small?
 *   2. What does it tell you when the sum is too large?
 */
export function pairSumSorted(
	nums: number[],
	target: number,
): [number, number] | null {
	// TODO: implement using one pointer at each end.
	throw new Error("Not implemented");
}
